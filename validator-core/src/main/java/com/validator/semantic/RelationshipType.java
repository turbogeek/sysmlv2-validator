package com.validator.semantic;

/**
 * Enumeration of relationship types in the SysML v2 RelationshipGraph.
 */
public enum RelationshipType {
    SPECIALIZES,
    REDEFINES,
    SUBSETS,
    SATISFIES,
    VERIFIES,
    ALLOCATES,
    OWNS,
    TYPES
}
